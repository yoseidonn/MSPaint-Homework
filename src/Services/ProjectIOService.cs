using MSPaint.Models;

namespace MSPaint.Services
{
    public class ProjectIOService
    {
        public void Save(string path, PixelGrid grid)
        {
            // placeholder
        }

        public PixelGrid? Load(string path)
        {
            return null;
        }
    }
}