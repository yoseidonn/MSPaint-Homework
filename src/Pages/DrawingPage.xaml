<UserControl x:Class="MSPaint.Pages.DrawingPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MSPaint.Controls">
    <Grid Background="#FFE0E0E0">
        <!-- Canvas container with border and checkerboard background -->
        <Border x:Name="CanvasBorder" 
                BorderBrush="#FF808080" 
                BorderThickness="2" 
                Background="#FFFFFFFF"
                HorizontalAlignment="Left" 
                VerticalAlignment="Top"
                Margin="0">
            <!-- Checkerboard pattern for transparent areas -->
            <Grid>
                <Grid.Background>
                    <DrawingBrush TileMode="Tile" Viewport="0,0,8,8" ViewportUnits="Absolute">
                        <DrawingBrush.Drawing>
                            <GeometryDrawing>
                                <GeometryDrawing.Geometry>
                                    <RectangleGeometry Rect="0,0,8,8"/>
                                </GeometryDrawing.Geometry>
                                <GeometryDrawing.Brush>
                                    <SolidColorBrush Color="#FFC0C0C0"/>
                                </GeometryDrawing.Brush>
                            </GeometryDrawing>
                        </DrawingBrush.Drawing>
                    </DrawingBrush>
                </Grid.Background>
                <!-- Canvas control -->
                <local:DoubleBufferedCanvasControl x:Name="CanvasControl"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>