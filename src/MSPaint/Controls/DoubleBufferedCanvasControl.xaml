<UserControl x:Class="MSPaint.Controls.DoubleBufferedCanvasControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MSPaint.Controls">
    <Grid Background="Transparent">
        <!-- Back buffer: actual image -->
        <Image x:Name="BackImage" Stretch="None"/>
        <!-- Front buffer: transparent preview layer -->
        <Image x:Name="FrontImage" Stretch="None"/>
    </Grid>
</UserControl>